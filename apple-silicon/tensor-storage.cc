// tensor-storage.h is header-only; this TU exists as a placeholder
// so the build system has something to compile (some generators
// warn on libraries with no TU contributions from a subdirectory).

#include "apple-silicon/tensor-storage.h"

namespace vpipe {

// Intentionally empty.

}
